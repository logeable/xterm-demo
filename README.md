# xterm-demo

使用方式：
```text
Usage of xterm-demo:
  -addr string
        ssh server address (default "localhost:22")
  -enc string
        term encoding: [gbk]
  -l string
        listen address (default ":9930")
  -p string
        password
  -u string
        username (default "root")

```